import styled from 'styled-components/native';
import { globalTheme } from '../../globalTheme';

export const Container = styled.Pressable`
    width: 100%;
    height: 100%;

    background-color: ${globalTheme.COLORS.GRAY_00};
`